# Lectio AI Camera System Requirements - PART 1: Core System
# Part 1: Real-time face detection and multi-person tracking

# ============================================
# CORE REQUIREMENTS (Required)
# ============================================

# Core Computer Vision
opencv-python>=4.8.0          # Main computer vision library
mediapipe>=0.10.0              # Face detection (fallback)
numpy>=1.24.0                  # Numerical operations
scipy>=1.10.0                  # Scientific computing (Hungarian algorithm)

# Deep Learning Framework
torch>=2.0.0                   # PyTorch for neural networks
torchvision>=0.15.0            # Vision utilities

# Video Processing
imageio>=2.31.0                # Video I/O
imageio-ffmpeg>=0.4.8          # FFmpeg backend

# ============================================
# DETECTION BACKENDS (Choose based on needs)
# ============================================

# Option 1: MediaPipe (Lightweight, CPU-friendly) - DEFAULT
# Already included above

# Option 2: YOLOv8 (Fast, accurate)
ultralytics>=8.0.200           # YOLOv8 models

# Option 3: RetinaFace (Most accurate face detection)
# Option 4: Face Recognition (For face identification)
face-recognition>=1.3.0         # Face recognition library
dlib>=19.24.0                   # Required by face-recognition

# ============================================
# PERFORMANCE & OPTIMIZATION (Recommended)
# ============================================

# ONNX Runtime (Faster inference)
onnx>=1.14.0                   # ONNX model format
onnxruntime>=1.15.0            # CPU inference
onnxruntime-gpu>=1.15.0        # GPU inference (CUDA)

# NVIDIA GPU Acceleration (Optional)
# Install CUDA-enabled PyTorch: https://pytorch.org/get-started/locally/
# tensorrt>=8.5.0               # NVIDIA TensorRT (advanced)

# System Monitoring
psutil>=5.9.0                  # System resource monitoring
GPUtil>=1.4.0                  # GPU monitoring

# ============================================
# COMMUNICATION (For Part 2)
# ============================================
websockets>=11.0.0             # WebSocket communication
aiofiles>=23.0.0               # Async file operations

# ============================================
# DATA & LOGGING
# ============================================
Pillow>=10.0.0                 # Image processing
tqdm>=4.65.0                   # Progress bars
python-json-logger>=2.0.0      # Structured logging

# ============================================
# DEVELOPMENT & TESTING
# ============================================
pytest>=7.4.0                  # Testing framework
pytest-asyncio>=0.21.0         # Async testing
black>=23.0.0                  # Code formatting
flake8>=6.0.0                  # Linting

# ==========================================
# INSTALLATION INSTRUCTIONS
# ==========================================

# 1. Basic Installation (CPU-only):
#    pip install -r requirements.txt

# 2. With NVIDIA GPU (CUDA 11.8):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#    pip install -r requirements.txt

# 3. With NVIDIA GPU (CUDA 12.1):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#    pip install -r requirements.txt

# 4. Verify Installation:
#    python -c "import torch; print(f'PyTorch: {torch.__version__}')"
#    python -c "import cv2; print(f'OpenCV: {cv2.__version__}')"
#    python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}')"
